name: AI Python PR Review (Azure AI Foundry)

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  ai-python-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get changed Python files
        run: |
          git fetch origin main
          git diff --name-only origin/main...HEAD | grep '\.py$' > files.txt || true

      - name: Combine Python code
        run: |
          echo "" > code.txt
          if [ -s files.txt ]; then
            while read -r file; do
              echo "### File: $file" >> code.txt
              cat "$file" >> code.txt
              echo "" >> code.txt
            done < files.txt
          else
            echo "No Python files changed." >> code.txt
          fi

      - name: Build request.json
        run: |
          jq -n \
            --arg code "$(cat code.txt)" \
            '{
              model: "gpt-4o-mini",
              input: [
                {
                  role: "user",
                  content: "You are a strict Python code reviewer. Check ONLY syntax, indentation, imports, and runtime errors.\n\nPython code:\n\n" + $code
                }
              ],
              max_output_tokens: 500
            }' > request.json

      - name: Call Azure AI Foundry
        env:
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_KEY: ${{ secrets.AZURE_OPENAI_KEY }}
        run: |
          curl --max-time 30 -X POST "$AZURE_OPENAI_ENDPOINT" \
            -H "Content-Type: application/json" \
            -H "api-key: $AZURE_OPENAI_KEY" \
            -d @request.json \
            -o response.json

      - name: Extract AI response
        run: |
          jq -r '.output_text // "⚠️ No issues returned by AI."' response.json > comment.txt

      - name: Post comment to PR
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('comment.txt', 'utf8');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
